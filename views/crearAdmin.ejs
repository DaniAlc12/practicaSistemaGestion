<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creacion de admin</title>
</head>
<body>
    <% if(typeof errores !== 'undefined' && errores.length > 0) { %>
    <div style="background-color: #ffcccc; padding: 10px; border: 1px solid red; border-radius: 5px; margin-bottom: 20px;">
        <p style="color: red; font-weight: bold;">No se pudo añadir el libro:</p>
        <ul>
            <% errores.forEach(error => { %>
                <li style="color: red;"><%= error %></li>
            <% }) %>
        </ul>
    </div>
<% } %>
    <h1>Registro de usuarios</h1>
    <form action="/crearAdmin" method="POST">

        <label>Nombre de usuario:
            <input type="text" name="username" value="<%= username || '' %>" required><br><br>
        </label>

        <label>Contraseña:
            <input type="password" name="password" value="<%= password || '' %>" required><br><br>
        </label>

        <label>Correo electrónico:
            <input type="email" name="email" value="<%= email || '' %>" required><br><br>
        </label>

        <label>Rol:
            <select name="role" required>
                <option name="admin" value="admin">Admin</option>
                <option name="invitado" value="invitado">Invitado</option>
            </select>
            <br><br>
        </label>
        <button type="submit">Registrarse</button>
        <a href="/">Volver al inicio</a>
    </form>
</body>
</html>